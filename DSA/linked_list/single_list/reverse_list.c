#include<stdio.h>
struct st
{
	int roll;
	char name[20];
	float marks;
	struct st *next;
};

void add_begin(struct st**);
void print(struct st*);
void save(struct st*);

int main()
{
	char op;
	struct st* hptr=0;
	do
	{

	}
}
